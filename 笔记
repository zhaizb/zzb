set接口为Collection接口的子接口
该接口与List接口平级，且没有先后次序，不允许元素重复（常用作去重）

主要实现类  HashSet类  TreeSet类
前者底层使用哈希表进行数据管理，后者使用有序二叉树进行数据管理

常用方法
Iterator<E> iterator() - 获取当前集合中的迭代器对象，用于迭代/遍历集合中所有元素
其中Iterator<E>是个接口，常用方法有：
   boolean hasNext()  用于判断是否拥有可以访问的元素
   E next()   用于取出一个元素并指向下一个位置

元素放入原理
使用新元素调用hasCode（）方法得到该元素的哈希码值
将哈希码值交给哈希算法算出新元素在哈希表数组的索引位置
若该位置没有元素，则直接将新元素放入，若有元素，则用新元素与已有元素依次比较
若新元素与已有元素相等，则保留旧元素放弃新元素，若不相等则将新元素放入已有元素的后面
